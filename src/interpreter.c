#include <stdio.h>
#include "word.h"

W_List *word_tokenize(FILE *source) {
    W_List *words = list_init();
    int i = 0, nb_words = 0;
    char c;
    while (c != EOF) {
        while (c != '\n') {
            
        }
    }
    return words;
}